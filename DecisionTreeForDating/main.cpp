#include "DecisionTreeForDating.h"
#include <QtWidgets/QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    DecisionTreeForDating w;
    w.show();
    return a.exec();
}
